# 水环境风险管控系统 - 后端运行手册

## 一、环境要求

### 1.1 必要软件

| 软件 | 版本要求 | 说明 |
|------|---------|------|
| JDK | 1.8+ | Java 开发工具包 |
| Maven | 3.6+ | 项目构建工具 |
| MySQL | 5.7+ 或 8.0+ | 数据库 |
| Git | 最新版 | 版本控制（可选）|

### 1.2 验证环境

打开命令行终端，执行以下命令验证环境：

```bash
# 验证 Java 版本
java -version

# 验证 Maven 版本
mvn -version

# 验证 MySQL 版本
mysql --version
```

---

## 二、数据库配置

> **⚠️ 重要提示：** 以下操作会删除并重新创建 `water_data` 数据库，请确保已备份重要数据！

### 2.1 执行初始化脚本

初始化脚本会自动处理以下操作：
1. 关闭外键检查
2. 删除已存在的 `water_data` 数据库
3. 重新创建数据库
4. 创建所有表结构
5. 插入默认数据

**方式一：命令行执行（推荐）**

```bash
mysql -u root -p < back/backend/src/main/resources/db/complete_schema.sql
```

**方式二：进入 MySQL 后执行**

```bash
mysql -u root -p
```

进入 MySQL 后，直接复制 `complete_schema.sql` 文件的全部内容并执行。

**方式三：使用 source 命令**

```bash
mysql -u root -p -e "source back/backend/src/main/resources/db/complete_schema.sql"
```

### 2.2 验证表结构

```sql
USE water_data;
SHOW TABLES;
```

预期输出应包含以下表：

| 表名 | 说明 |
|------|------|
| users | 用户表 |
| reservoirs | 水库表 |
| patrol_records | 巡查记录表 |
| warning_records | 预警记录表 |
| warning_thresholds | 预警阈值配置表 |
| layer_configs | 图层配置表 |
| roles | 角色表 |
| simulation_configs | 模拟参数配置表 |
| annotations | 标注管理表 |
| water_situation | 水情管理表 |
| section_monitor | 监测断面管理表 |
| inspection_tasks | 巡查任务表 |
| task_feedbacks | 任务反馈表 |
| issue_reports | 问题上报表 |

### 2.3 修改数据库连接配置（可选）

如果需要修改数据库连接信息，编辑配置文件：

**文件位置：** `back/backend/src/main/resources/application.yml`

```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/water_data?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root          # 修改为你的用户名
    password: root           # 修改为你的密码
```

---

## 三、启动后端服务

### 3.1 使用 Maven 启动

```bash
# 进入后端目录
cd back/backend

# 清理并编译
mvn clean compile

# 启动应用
mvn spring-boot:run
```

### 3.2 使用 JAR 包启动

```bash
# 先打包
mvn clean package -DskipTests

# 启动
java -jar target/clarimire-backend-1.0.0.jar
```

### 3.3 启动成功验证

当看到以下日志时，表示启动成功：

```
========================================
  AquaGuardian Backend Started!
  Port: 8080
  API Base URL: http://localhost:8080
========================================
Tomcat started on port(s): 8080
```

---

## 四、验证接口可用性

### 4.1 基础测试

使用浏览器或 Postman 访问：

```bash
# 测试接口是否响应
GET http://localhost:8080/api/test/hello

# 预期返回：{"message": "Hello from Spring Boot!"}
```

### 4.2 API 测试工具

本项目提供了前端测试工具：

1. 进入前端目录：`cd frontend-test`
2. 安装依赖：`npm install`
3. 启动开发服务器：`npm run dev`
4. 访问：`http://localhost:3005`

---

## 五、默认账号

### 5.1 账号信息

| 用户名 | 密码 | 角色 | 说明 |
|--------|------|------|------|
| admin | 123456 | 管理员 | 系统管理员，拥有所有权限 |
| inspector1 | 123456 | 巡查员 | 水库巡查人员 |
| inspector2 | 123456 | 巡查员 | 水库巡查人员 |
| public1 | 123456 | 公众 | 普通公众用户 |

> **注意：** 密码 `123456` 的 MD5 值为 `e10adc3949ba59abbe56e057f20f883e`

### 5.2 测试登录

使用前端测试工具登录：

1. 进入"用户认证"模块
2. 使用默认账号登录：
   - 用户名：`admin`
   - 密码：`123456`
3. 点击"测试登录"
4. 成功后将获得 JWT Token

---

## 六、API 接口列表

### 6.1 认证接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/login | 用户登录 |
| POST | /api/auth/register | 用户注册 |
| GET | /api/auth/profile | 获取用户信息 |
| PUT | /api/auth/password | 修改密码 |

### 6.2 用户管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/users | 获取用户列表 |
| GET | /api/users/{id} | 获取用户详情 |
| POST | /api/users | 创建用户 |
| PUT | /api/users/{id} | 更新用户 |
| DELETE | /api/users/{id} | 删除用户 |

### 6.3 Dashboard

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/dashboard/stats | 获取统计数据 |
| GET | /api/dashboard/water-quality/trend | 水质趋势 |
| GET | /api/dashboard/warnings/latest | 最新预警 |

### 6.4 巡查管理

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/patrol/checkin | 签到 |
| GET | /api/patrol/records | 巡查记录列表 |
| POST | /api/patrol/report | 问题上报 |

### 6.5 预警管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/warnings | 预警列表 |
| GET | /api/warnings/{id} | 预警详情 |
| PUT | /api/warnings/{id}/status | 更新预警状态 |

### 6.6 图层配置

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/layer-configs | 获取图层配置 |
| PUT | /api/layer-configs | 更新图层配置 |

### 6.7 角色权限

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/roles | 获取角色列表 |
| GET | /api/roles/{id} | 获取角色详情 |
| POST | /api/roles | 创建角色 |
| PUT | /api/roles/{id} | 更新角色 |
| DELETE | /api/roles/{id} | 删除角色 |

### 6.8 模拟配置

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/simulation-configs | 获取所有配置 |
| GET | /api/simulation-configs/default | 获取默认配置 |
| POST | /api/simulation-configs | 创建配置 |
| PUT | /api/simulation-configs/{id} | 更新配置 |
| DELETE | /api/simulation-configs/{id} | 删除配置 |

### 6.9 水库管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/reservoirs | 获取水库列表 |
| GET | /api/reservoirs/{id} | 获取水库详情 |
| POST | /api/reservoirs | 创建水库 |
| PUT | /api/reservoirs/{id} | 更新水库 |
| DELETE | /api/reservoirs/{id} | 删除水库 |

### 6.10 地图要素

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/map/features | 获取地图要素 |
| GET | /api/map/features/reservoirs | 获取水库点位 |
| GET | /api/map/features/monitor-points | 获取监测点 |

### 6.11 水情管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/waterSituation/list | 水情列表 |
| POST | /api/waterSituation/create | 创建水情 |
| POST | /api/waterSituation/update | 更新水情 |
| DELETE | /api/waterSituation/delete/{id} | 删除水情 |
| POST | /api/waterSituation/import | 导入 Excel |
| GET | /api/waterSituation/export | 导出 Excel |

---

## 七、常见问题

### 7.1 端口被占用

如果 8080 端口被占用，修改 `application.yml` 中的端口：

```yaml
server:
  port: 8081  # 改为其他端口
```

### 7.2 数据库连接失败

1. 检查 MySQL 服务是否启动
2. 验证用户名密码是否正确
3. 确认数据库 `water_data` 是否存在
4. 检查是否有连接权限

```bash
# Windows 检查 MySQL 服务
net start | findstr MySQL

# Linux/Mac 检查 MySQL 服务
sudo systemctl status mysql
```

### 7.3 Maven 依赖下载失败

配置 Maven 镜像源（可选）：

在 `~/.m2/settings.xml` 中添加：

```xml
<mirrors>
  <mirror>
    <id>aliyun</id>
    <name>Aliyun Maven</name>
    <url>https://maven.aliyun.com/repository/public</url>
    <mirrorOf>central</mirrorOf>
  </mirror>
</mirrors>
```

### 7.4 JDK 版本不兼容

确保 JDK 版本为 1.8：

```bash
# Windows
set JAVA_HOME=C:\Program Files\Java\jdk1.8.0_xxx
set PATH=%JAVA_HOME%\bin;%PATH%

# Linux/Mac
export JAVA_HOME=/usr/lib/jvm/java-8-openjdk
export PATH=$JAVA_HOME/bin:$PATH
```

---

## 八、项目结构

```
f:\AAA\
├── back\                          # 后端代码
│   └── backend\
│       ├── pom.xml               # Maven 配置
│       └── src\
│           └── main\
│               ├── java\com\clarimire\
│               │   ├── controller\   # 控制器
│               │   ├── service\       # 服务层
│               │   ├── mapper\        # 数据访问层
│               │   ├── entity\        # 实体类
│               │   ├── config\        # 配置类
│               │   └── util\         # 工具类
│               └── resources\
│                   ├── application.yml
│                   └── db\            # 数据库脚本
│                       └── complete_schema.sql
├── frontend-test\                  # 前端测试工具
│   ├── package.json
│   ├── vite.config.js
│   └── index.html
└── README.md                       # 本文档
```

---

## 九、联系方式

如有问题，请检查：

1. 日志输出信息
2. 控制台错误提示
3. 网络请求响应

祝您使用愉快！
