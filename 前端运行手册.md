# 水环境风险管控系统 - 前端运行手册

## 一、环境要求

### 1.1 必要软件

| 软件 | 版本要求 | 说明 |
|------|----------|------|
| Node.js | 14+ | JavaScript 运行环境 |
| npm / yarn | 最新版 | 包管理工具 |

### 1.2 验证环境

```bash
# 验证 Node.js 版本
node -v

# 验证 npm 版本
npm -v
```

## 二、安装依赖

```bash
cd back/frontend

# 使用 npm
npm install

# 或使用 yarn
yarn install
```

## 三、启动开发服务器

```bash
# npm
npm run serve

# yarn
yarn serve
```

启动成功后访问: http://localhost:8081

## 四、构建生产版本

```bash
# npm
npm run build

# yarn
yarn build
```

构建产物位于 `dist/` 目录。

## 五、配置说明

### 5.1 API 代理配置

项目使用 Vite 开发服务器，API 请求会代理到后端。

配置文件: `vite.config.js`

```javascript
// 当前代理配置将请求转发到 http://localhost:8080
// 如需修改，请编辑 vite.config.js
```

### 5.2 环境变量

如需配置 API 地址，创建 `.env` 文件:

```env
VITE_API_BASE_URL=http://localhost:8080
```

## 六、常见问题

### 6.1 端口被占用

如果 8081 端口被占用，修改 `package.json` 中的端口或使用 `--port` 参数:

```bash
npm run serve -- --port 8082
```

### 6.2 依赖安装失败

```bash
# 清除缓存后重试
npm cache clean --force
rm -rf node_modules
npm install
```

### 6.3 编译错误

```bash
# 检查 Node.js 版本
node -v  # 需要 14+

# 重新安装依赖
rm -rf node_modules package-lock.json
npm install
```

### 6.4 跨域问题

前端已配置 API 代理，确保后端 CORS 配置包含 8081 端口。

后端 CORS 配置位于: `back/backend/src/main/java/com/clarimire/config/WebConfig.java`

## 七、项目依赖

### 主要依赖

| 依赖 | 版本 | 说明 |
|------|------|------|
| vue | 3.2.13 | 核心框架 |
| element-plus | 2.2.0 | UI 组件库 |
| axios | 0.27.2 | HTTP 客户端 |
| echarts | 5.3.2 | 图表库 |
| vue-router | 4.0.3 | 路由管理 |
| vuex | 4.0.0 | 状态管理 |

## 八、目录结构

```
back/frontend/
├── public/                 # 静态资源
│   └── Beijing-GeoJson/   # 地图GeoJSON数据
├── src/
│   ├── api/               # API 接口封装
│   ├── assets/            # 资源文件
│   ├── components/        # 公共组件
│   ├── router/            # 路由配置
│   ├── store/             # Vuex 状态管理
│   ├── utils/             # 工具函数
│   ├── views/             # 页面组件
│   ├── App.vue            # 根组件
│   └── main.js            # 入口文件
├── package.json
├── vite.config.js         # Vite 配置
├── vue.config.js          # Vue CLI 配置
└── babel.config.js       # Babel 配置
```
