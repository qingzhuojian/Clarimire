<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clarimire.mapper.WaterSituationMapper">
    <resultMap id="BaseResultMap" type="com.clarimire.entity.WaterSituation">
        <id column="id" property="id"/>
        <result column="reservoir_name" property="reservoirName"/>
        <result column="date" property="date"/>
        <result column="water_level" property="waterLevel"/>
        <result column="storage" property="storage"/>
        <result column="avg_inflow" property="avgInflow"/>
        <result column="avg_outflow" property="avgOutflow"/>
        <result column="yoy_increase" property="yoyIncrease"/>
        <result column="total_capacity" property="totalCapacity"/>
        <result column="flood_level" property="floodLevel"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM water_situation WHERE 1=1
        <if test="reservoirName != null and reservoirName != ''">
            AND reservoir_name = #{reservoirName}
        </if>
        <if test="startDate != null and startDate != ''">
            AND date >= #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            AND date &lt;= #{endDate}
        </if>
        ORDER BY date DESC, reservoir_name
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM water_situation WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO water_situation (reservoir_name, date, water_level, storage, avg_inflow, avg_outflow, yoy_increase, total_capacity, flood_level)
        VALUES (#{reservoirName}, #{date}, #{waterLevel}, #{storage}, #{avgInflow}, #{avgOutflow}, #{yoyIncrease}, #{totalCapacity}, #{floodLevel})
    </insert>

    <insert id="insertBatch">
        INSERT INTO water_situation (reservoir_name, date, water_level, storage, avg_inflow, avg_outflow, yoy_increase, total_capacity, flood_level)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.reservoirName}, #{item.date}, #{item.waterLevel}, #{item.storage}, #{item.avgInflow}, #{item.avgOutflow}, #{item.yoyIncrease}, #{item.totalCapacity}, #{item.floodLevel})
        </foreach>
    </insert>

    <update id="update">
        UPDATE water_situation SET
            reservoir_name = #{reservoirName},
            date = #{date},
            water_level = #{waterLevel},
            storage = #{storage},
            avg_inflow = #{avgInflow},
            avg_outflow = #{avgOutflow},
            yoy_increase = #{yoyIncrease},
            total_capacity = #{totalCapacity},
            flood_level = #{floodLevel}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM water_situation WHERE id = #{id}
    </delete>

    <delete id="deleteBatch">
        DELETE FROM water_situation WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectDistinctReservoirs" resultType="string">
        SELECT DISTINCT reservoir_name FROM water_situation ORDER BY reservoir_name
    </select>
</mapper>
