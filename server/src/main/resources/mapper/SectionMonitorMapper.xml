<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clarimire.mapper.SectionMonitorMapper">
    <resultMap id="BaseResultMap" type="com.clarimire.entity.SectionMonitor">
        <id column="id" property="id"/>
        <result column="monitor_point_name" property="monitorPointName"/>
        <result column="reservoir_name" property="reservoirName"/>
        <result column="year" property="year"/>
        <result column="month" property="month"/>
        <result column="oxygen" property="oxygen"/>
        <result column="potassium_permanganate" property="potassiumPermanganate"/>
        <result column="cod" property="cod"/>
        <result column="flow" property="flow"/>
        <result column="water_depth" property="waterDepth"/>
        <result column="total_nitrogen" property="totalNitrogen"/>
        <result column="total_phosphorus" property="totalPhosphorus"/>
        <result column="ammonia_nitrogen" property="ammoniaNitrogen"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM section_monitor WHERE 1=1
        <if test="reservoirName != null and reservoirName != ''">
            AND reservoir_name = #{reservoirName}
        </if>
        <if test="year != null">
            AND year = #{year}
        </if>
        <if test="month != null">
            AND month = #{month}
        </if>
        ORDER BY year DESC, month DESC, reservoir_name
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM section_monitor WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO section_monitor (monitor_point_name, reservoir_name, year, month, oxygen, potassium_permanganate, cod, flow, water_depth, total_nitrogen, total_phosphorus, ammonia_nitrogen)
        VALUES (#{monitorPointName}, #{reservoirName}, #{year}, #{month}, #{oxygen}, #{potassiumPermanganate}, #{cod}, #{flow}, #{waterDepth}, #{totalNitrogen}, #{totalPhosphorus}, #{ammoniaNitrogen})
    </insert>

    <insert id="insertBatch">
        INSERT INTO section_monitor (monitor_point_name, reservoir_name, year, month, oxygen, potassium_permanganate, cod, flow, water_depth, total_nitrogen, total_phosphorus, ammonia_nitrogen)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.monitorPointName}, #{item.reservoirName}, #{item.year}, #{item.month}, #{item.oxygen}, #{item.potassiumPermanganate}, #{item.cod}, #{item.flow}, #{item.waterDepth}, #{item.totalNitrogen}, #{item.totalPhosphorus}, #{item.ammoniaNitrogen})
        </foreach>
    </insert>

    <update id="update">
        UPDATE section_monitor SET
            monitor_point_name = #{monitorPointName},
            reservoir_name = #{reservoirName},
            year = #{year},
            month = #{month},
            oxygen = #{oxygen},
            potassium_permanganate = #{potassiumPermanganate},
            cod = #{cod},
            flow = #{flow},
            water_depth = #{waterDepth},
            total_nitrogen = #{totalNitrogen},
            total_phosphorus = #{totalPhosphorus},
            ammonia_nitrogen = #{ammoniaNitrogen}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM section_monitor WHERE id = #{id}
    </delete>

    <delete id="deleteBatch">
        DELETE FROM section_monitor WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectDistinctReservoirs" resultType="string">
        SELECT DISTINCT reservoir_name FROM section_monitor ORDER BY reservoir_name
    </select>

    <select id="selectDistinctYears" resultType="int">
        SELECT DISTINCT year FROM section_monitor ORDER BY year DESC
    </select>
</mapper>
