<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clarimire.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.clarimire.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="phone" property="phone"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="allow_remote_checkin" property="allowRemoteCheckin"/>
        <result column="mobile_enabled" property="mobileEnabled"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM user
        <where>
            <if test="role != null and role != ''">AND role = #{role}</if>
        </where>
        ORDER BY id
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, real_name, phone, role, status, allow_remote_checkin, mobile_enabled)
        VALUES (#{username}, #{password}, #{realName}, #{phone}, #{role}, #{status}, #{allowRemoteCheckin}, #{mobileEnabled})
    </insert>

    <update id="update">
        UPDATE user SET
            real_name = #{realName},
            phone = #{phone},
            role = #{role},
            status = #{status},
            allow_remote_checkin = #{allowRemoteCheckin},
            mobile_enabled = #{mobileEnabled}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>
</mapper>
