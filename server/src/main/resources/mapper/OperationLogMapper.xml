<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clarimire.mapper.OperationLogMapper">
    <resultMap id="BaseResultMap" type="com.clarimire.entity.OperationLog">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="operation" property="operation"/>
        <result column="module" property="module"/>
        <result column="detail" property="detail"/>
        <result column="ip" property="ip"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM operation_log WHERE 1=1
        <if test="username != null and username != ''">
            AND username = #{username}
        </if>
        <if test="startDate != null and startDate != ''">
            AND create_time >= #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            AND create_time &lt;= #{endDate}
        </if>
        ORDER BY create_time DESC
    </select>

    <insert id="insert">
        INSERT INTO operation_log (username, operation, module, detail, ip)
        VALUES (#{username}, #{operation}, #{module}, #{detail}, #{ip})
    </insert>

    <delete id="deleteById">
        DELETE FROM operation_log WHERE id = #{id}
    </delete>
</mapper>
