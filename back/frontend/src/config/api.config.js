// API 基础配置 - 移植时只需修改此文件
const API_CONFIG = {
  // 后端服务器地址，移植时修改此处的 IP 或域名
  baseURL: 'http://localhost:8080',
  
  // 可选：如果是部署在服务器上，修改为实际域名或 IP
  // 例如：'http://192.168.1.100:8080' 或 'http://your-domain.com'
}

export default API_CONFIG
