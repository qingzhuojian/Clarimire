# Web 端天地图（可选）

Web 管理端地图可使用天地图（与 GeoScene 业务层坐标更易对齐）。
移动端 H5 已改用高德瓦片，**不依赖**本配置。

1. 申请：https://console.tianditu.gov.cn/
2. 复制 `apps/web/.env.example` 为 `.env`，填写 `VITE_TIANDITU_TOKEN`
3. 重启 `npm run dev`

底图空白：检查 Token、域名白名单、是否重启前端。
